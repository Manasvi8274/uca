var numarra1 = [9,8,7,6,5];

// for loop
for(var i=0;i<numarra1.length;i++){
    console.log(`value,${numarra1[i]}`);
}

// array.map(param) =>()
    // return a new Array
// spread operator = ...(three dots) which spreads  the array\
// rest operator = ...arg where ...arg is used to take n number of arument inside a function
